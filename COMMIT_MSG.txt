fix: audit and resolve all cross-cutting inconsistencies

1. apps/web/next.config.js - add output standalone (Docker web build was broken)
2. docker-compose.yml - rename PAYMENT_RECIPIENT_ADDRESS to TREASURY_ADDRESS;
   forward all blockchain env vars to api container; add WalletConnect build arg;
   fix default NEXT_PUBLIC_CHAIN_ID to 11155111 (Sepolia)
3. apps/web/Dockerfile - expose NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID in build stage
4. payment-session.service.ts - createSession now returns treasuryAddress and
   chainName even when returning an existing PENDING session (frontend was crashing)
5. apps/web/src/lib/wagmi.ts - add sepolia, base, optimism, polygonAmoy chains
   (dev payment flow on Sepolia was silently failing, chain was not configured)
6. PaymentSessionModal.tsx - replace hardcoded etherscan.io URL with per-chain
   explorer map; fix useEffect deps to [purpose, chainId]
7. chain.service.ts and chain.config.ts - replace deprecated polygonMumbai (80001)
   with polygonAmoy (80002); Mumbai testnet is shut down
8. apps/api/entrypoint.sh + Dockerfile - run prisma migrate deploy before server
   start so production containers auto-apply pending migrations
9. .env.example - add NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID; fix chain ID default;
   add Polygon Amoy RPC entry
